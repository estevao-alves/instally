<UserControl x:Class="InstallyApp.Components.Layout.Footer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InstallyApp.Components.Layout"
             mc:Ignorable="d"
             d:DesignWidth="800"
             >
    <Grid
        MinHeight="60"
        Background="#2F2F2F"
        >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Horizontal" Name="ListaDeInstalacao" Margin="10" />

        <Border
            Style="{StaticResource ActionButton}"
            MouseDown="VerificarApps_MouseDown"
            Name="InstallButton"
            Grid.Column="2"
            Margin="20 10"
            >
            <TextBlock
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                FontSize="20"
                FontWeight="Bold"
                Text="Install"
                />
        </Border>
        
    </Grid>
</UserControl>

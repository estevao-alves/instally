<UserControl x:Class="WindowsGuide_WPF.Components.MenuAppItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    
    <Grid Margin="0 0 0 3" MouseDown="Grid_MouseDown">
        <Border Name="BorderWrapper" Background="Transparent" CornerRadius="8"/>
        <DockPanel Height="30" Margin="12, 5" VerticalAlignment="Center">

            <Grid>
                <Viewbox Name="WrapperAppIcon" Width="20" Height="20">
                    <Border
                    Width="18"
                    Height="18"
                    CornerRadius="18"
                    Background="{DynamicResource PrimaryColor}"
                    />
                </Viewbox>
            </Grid>

            <StackPanel Name="MeusIcons" />

            <TextBlock
                Padding="2"
                Margin="10,0,0,0"
                Foreground="White"
                Name="Title"
                Text="Nome do App"
                FontSize="16"
                VerticalAlignment="Center"
                />
        </DockPanel>
    </Grid>
</UserControl>
